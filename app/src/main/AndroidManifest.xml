<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cellsystem">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CrudAbx">
        <activity
            android:name="com.abx.cellsystem.CelularFormActivity"
            android:label="@string/title_form_celular"
            android:exported="false" />
        <activity
            android:name="com.abx.cellsystem.ListActivity"
            android:label="@string/title_detail_celular"
            android:exported="false" />
        <activity
            android:name="com.abx.cellsystem.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
    <queries>
        <intent>
            <action android:name="android.intent.action.DIAL" />
        </intent>
    </queries>
    <uses-permission android:name="android.permission.CALL_PHONE" />
</manifest>